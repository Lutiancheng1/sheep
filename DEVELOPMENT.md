# 羊了个羊项目 - 开发指南

## 项目概况

✅ **已完成**：
- Monorepo 项目结构搭建（使用 npm workspaces）
- 前端项目初始化（Next.js 14 + Phaser 3）
- 后端项目初始化（NestJS）
- 管理后台初始化（Vite + React + Ant Design）
- 共享类型包创建
- Phaser 3 核心游戏 Demo 编写完成（包含完整的叠层三消逻辑）

## ⚠️ 当前问题：Node.js 版本要求

您的系统当前使用 **Node.js v16.20.2**，但项目需要 **Node.js >= 18.17.0** 来运行 Next.js 14。

### 解决方案（请选择一个）

#### 选项 1：升级 Node.js（推荐）

使用 nvm（Node Version Manager）升级：

```bash
# 安装 nvm（如果还没有）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 安装 Node.js 20（LTS）
nvm install 20
nvm use 20

# 验证版本
node --version  # 应该显示 v20.x.x
```

然后重新安装依赖并启动：
```bash
cd "/Users/lutiancheng/Downloads/Sheep a Sheep"
npm install
cd packages/frontend && npm run dev
```

#### 选项 2：降级 Next.js 版本

修改为兼容 Node 16 的旧版本（不推荐，会缺少新特性）：

```bash
cd packages/frontend
npm install next@13.5.0 --save
npm run dev
```

#### 选项 3：仅运行 Phaser Demo（临时方案）

不使用 Next.js，直接用简单的 HTML 页面测试 Phaser：
我可以创建一个独立的 HTML 文件来演示游戏。

---

## 项目结构

```
sheep-game/
├── packages/
│   ├── frontend/           # ✅ 游戏客户端
│   │   ├── src/
│   │   │   ├── app/
│   │   │   │   └── page.tsx          # 主页面
│   │   │   └── components/
│   │   │       ├── PhaserGame.tsx    # Phaser 集成组件
│   │   │       └── scenes/
│   │   │           └── GameScene.ts  # 核心游戏逻辑
│   │   └── package.json
│   ├── backend/            # ✅ NestJS 后端
│   ├── admin/              # ✅ 管理后台
│   └── shared/             # ✅ 共享类型
└── package.json
```

## 游戏功能演示

当前 Phaser Demo 已实现：
- ✅ 3层叠放的方块（emoji 图标：🐑🐄🐷🐔🐶）
- ✅ 点击最顶层方块移动到槽位
- ✅ 7个槽位系统
- ✅ 三消检测与消除动画
- ✅ 分数系统
- ✅ 胜利/失败判定
- ✅ 重新开始功能

## 下一步

1. **解决 Node.js 版本问题**（见上面的选项）
2. 启动开发服务器查看游戏
3. 继续实现更多游戏功能

请告诉我您选择哪个方案，我会协助您继续！
